import PlaySessionPage, { type PlaySessionPageProps } from '@/features/PlaySession/page/PlaySessionPage';

export default function PlaySession(props: PlaySessionPageProps) {
  return <PlaySessionPage {...props} />;
}


