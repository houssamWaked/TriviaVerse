import React from 'react';
import ClassicPage, { type ClassicProps } from '@/features/Classic/page/ClassicPage';

export default function Classic(props: ClassicProps) {
  return <ClassicPage {...props} />;
}

